[settings]
# Don't let mise manage Node.js - we use nvm for that
disable_tools = ["node"]

# Auto-install missing tool versions when entering directories
auto_install = true

# Show status message when tools are missing
status = { missing_tools = "always", show_tools = true }

[tools]
java = "temurin-21"
